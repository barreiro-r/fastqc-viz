% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_status_summary.R
\name{plot_status_summary}
\alias{plot_status_summary}
\title{FastQC-viz: Status Summary}
\usage{
plot_status_summary(fastqc_data)
}
\arguments{
\item{fastqc_data}{output from fastqc parser}
}
\value{
html table;
}
\description{
Generates an HTML summary table of the pass/warn/fail status for each
FastQC module.
}
\details{
This function creates a concise, two-column HTML table that visually
summarizes the results of a FastQC analysis. It is designed to work with
data parsed by the \code{parse_fastqc()} function.

The function transforms the FastQC data by:
\enumerate{
\item Extracting the \code{status} ("pass", "warn", or "fail") for each analysis module.
\item Using the \code{status_to_pill()} helper function to convert each status string
into a colored HTML badge for easy visual identification.
\item Cleaning up module names (e.g., "per_base_sequence_quality" becomes
"Per base sequence quality").
\item Rendering the final output as a clean, headerless HTML table using the
\code{kableExtra} package.
}
}
\examples{
fastqc_data <- parse_fastqc(system.file("extdata", "SRR622457_2_fastqc.txt", package = "fastqcviz"))
plot_status_summary(fastqc_data)

}
\keyword{html_formater}
\keyword{internal}
